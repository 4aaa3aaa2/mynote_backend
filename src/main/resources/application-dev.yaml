spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/aaa_mynote?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Tokyo
    username: root
    password: aaa432771984

upload:
  path: ${user.dir}/tmp/uploads # 指定上传文件的存储路径
  url-prefix: http://localhost:8080/images  # 访问上传文件的URL前缀
